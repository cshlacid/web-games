# CLAUDE.md

**여러 개의 미니 웹게임을 한 저장소에서 관리한다.**
서로 독립적인 게임들이 한 저장소에 모여 있는 형태다.

## 언어

- **모든 대화와 코드 주석은 한국어로 작성한다.** 커밋 메시지와 PR 본문도 한국어를
  기본으로 한다.
- 식별자(변수·함수·파일명)는 영어를 유지한다. 언어 규칙은 사람이 읽는 산문에
  적용되며, 코드 자체에는 적용되지 않는다.

## 주석 및 문서 작성 원칙

- **주석은 자명하지 않은 이유(why)만 적는다.** 코드가 무엇을 하는지는 코드가 이미
  말하고 있으므로 다시 적지 않는다. 그 선택을 한 이유, 시도했지만 실패한 대안,
  겉보기에 불필요해 보이는 코드가 필요한 이유가 주석의 대상이다.
- **이 파일도 같은 규칙을 따른다.** 트리를 열어 보면 알 수 있는 것(디렉터리 목록,
  설치된 의존성, 존재하는 파일), 설정 파일에 이미 적혀 있는 것은 여기에 옮겨
  적지 않는다. 이 파일에는 코드를 읽어도 알 수 없는 것만 남긴다 — 결정의 배경,
  아직 정해지지 않은 것, 저장소 밖의 워크플로.
- **이 파일에는 저장소 전체에 걸리는 것만 적는다. 게임 하나에만 해당하는 것은
  `games/<이름>/CLAUDE.md`에 적는다.**
- 규칙을 무효화하는 변경을 할 때는 같은 커밋에서 해당 파일을 고친다.

## 출력 최소화

- 코드 수정은 반드시 Write/Edit 도구로 직접 파일에 적용한다.
- 수정한 코드 전체를 채팅에 출력하지 않는다.
- 전체 파일 내용이나 전체 diff를 채팅에 출력하지 않는다.
- 새 파일도 반드시 도구로 직접 생성하고 내용을 채팅에 붙여넣지 않는다.
- 작업 완료 후에는 변경된 파일 경로와 변경 요약만 1~3문장으로 출력한다.
- 사용자가 요청하지 않은 설명, 예제 코드, 사용법, 배경 설명은 출력하지 않는다.

## 파일 및 컨텍스트 최소화

- 먼저 필요한 파일만 읽는다.
- 작업과 무관한 파일은 읽지 않는다.
- 이미 확인한 파일은 불필요하게 다시 읽지 않는다.
- 대규모 파일 전체를 읽기보다 필요한 부분만 확인한다.
- 검색 범위를 가능한 한 구체적인 디렉터리와 파일로 제한한다.
- 작업과 관련 없는 프로젝트 전체 탐색을 하지 않는다.

## 서브 에이전트 사용 규칙

- 서브 에이전트는 작업에 실질적인 이점이 있을 때만 사용한다.
- 단순한 단일 파일 수정, 간단한 버그 수정, 명확한 작업은 메인 에이전트가 직접 처리한다.
- 여러 파일이나 여러 영역을 독립적으로 조사해야 할 때 서브 에이전트를 우선 고려한다.
- 코드베이스 탐색, 원인 분석, 테스트 조사 등 시간이 많이 걸리는 탐색 작업은 서브 에이전트 사용을 고려한다.
- 서로 독립적인 작업은 가능하면 서브 에이전트로 병렬 처리한다.
- 메인 에이전트와 서브 에이전트가 동일한 파일을 동시에 수정하지 않는다.
- 서브 에이전트에게 불필요하게 전체 프로젝트를 탐색하도록 요청하지 않는다.
- 서브 에이전트에게 필요한 디렉터리, 파일, 검색 범위를 명확하게 지정한다.

## 서브 에이전트 출력 규칙

- 서브 에이전트는 장황한 설명을 하지 않는다.
- 조사 결과는 핵심 결론과 필요한 파일 경로만 반환한다.
- 전체 파일 내용이나 전체 diff를 반환하지 않는다.
- 발견한 문제는 다음 형식으로 간결하게 보고한다:
  - 결론
  - 관련 파일
  - 필요한 변경
  - 주의사항
- 조사 결과가 없으면 "특이사항 없음"만 반환한다.
- 메인 에이전트가 추가로 확인할 필요가 없는 수준으로 결과를 요약한다.

## 작업 방식

- 작업을 시작하기 전에 가장 작은 범위의 필요한 파일을 식별한다.
- 가능한 경우 검색 → 필요한 파일 확인 → 수정 → 테스트 순서로 진행한다.
- 작은 수정은 최소 범위로 변경한다.
- 이미 해결된 문제를 다시 조사하지 않는다.
- 작업 완료 후 불필요한 재검증이나 반복 설명을 하지 않는다.

## 프로젝트 성격

- **스택은 게임마다 다를 수 있다.**
- 새 게임의 스택은 그 게임에 맞는 것을 고르되, 정하기 전에 사용자와 합의한다.
- **혼자 가볍게 만들 수 있는 규모의 단일 플레이어 게임**이 대상이다.
- 게임 하나를 추가하는 것이 기본 작업 단위다. 여러 게임을 동시에 건드리는 변경은
  피한다.
- 게임 하나는 `games/<이름>/` 폴더 하나에 담기고, 루트 `index.html`이 게임 목록
  페이지다. **게임을 추가하면 목록 페이지에도 카드를 추가한다**
- 스택은 게임마다 달라도 **헤더 배치는 모든 게임이 같다**: 왼쪽 위에 `← 목록`(도움말이
  있으면 그 옆), 가운데에 게임 이름, 오른쪽에 소리 설정. 나머지는 그 아래 줄에 둔다.
- `shared/`는 여러 게임에서 반복되는 경우 공통 부분을 분리한다.

## 배포

- GitHub Pages로 배포한다.
- **경로는 상대경로로 쓴다.**

## 명령

**사이트에는 패키지 매니저도 빌드 도구도 없다.** 받아서 열면 그대로 돈다.
실시간으로 만들기에는 너무 오래 걸리는 자료는 미리 만들어 둔다.

**게임별 테스트와 굽는 명령은 그 게임의 `CLAUDE.md`에 있다.** 테스트도 그 게임
폴더 안에 둔다. 새 게임을 만들면 그 폴더에 `CLAUDE.md`를 두고 거기에 적는다.
린트·포매터는 아직 없다.

## 규칙

- **게임 로직과 화면을 파일로 분리한다.** 브라우저 없이 `node`로 규칙을 검증하기
  위해서다. 로직이 DOM을 만지는 순간 이 테스트가 불가능해진다.
- 로직 모듈 끝의 `module.exports` / `window.X` 이중 노출은 그 테스트를 위한
  통로다. 빌드 도구 없이 node와 브라우저 양쪽에서 쓰려면 이 방법이 필요하다.
- **소리는 음원 파일이 아니라 Web Audio로 합성하고, 그림은 인라인 SVG로 직접
  그린다.** 저장소에 바이너리 애셋을 들이지 않고, 빌드 없이 정적 파일만 배포하는
  구조를 유지하기 위해서다.
- **예외가 하나 있다: 힐러의 주인공 그림**(`games/healer/hero.png`). 사람이 그린
  시안을 그대로 쓰기로 하면서 도형을 포기한 자리다. 규칙을 없앤 것이 아니라
  **시안을 받은 캐릭터만 그림 파일을 쓴다** — 나머지는 여전히 도형이고, 새 그림을
  도형으로 그릴 수 없을 때에만 여기에 더한다. 그림 파일을 들일 때 지키는 것:
  프레임을 격자 한 장에 담아 요청을 하나로 두고, 화면에서 쓰는 크기의 두 배쯤에서
  자르며(주인공은 칸 98×88, 화면 67px), 어느 화면에 어떤 칸을 쓰는지는 자료로
  적어 둔다(`sprites.js`의 `SHEETS`). 소리는 예외가 없다.
- **아이콘도 그림이라 이모지나 기호 문자로 대신하지 않는다.** 글꼴이 그리는
  그림이라 기기마다 모양과 크기가 다르고(⌫와 ↶는 아예 안 그리는 글꼴도 있다),
  제 색을 들고 와서 화면이 정한 색 규칙을 깬다. `currentColor`로 그으면 밝은
  테마와 어두운 테마를 함께 탄다. 목록 카드와 파비콘, 게임 안의 도구 단추와
  자판 키가 전부 이 규칙을 따른다.
- **아이콘이 사는 곳이 셋이다.** 여러 게임이 함께 쓰는 것(연필·지우기·되돌리기·
  힌트·새 판·다시·다음·시프트)은 `shared/icons.js`, 그 게임에만 있는 것은 게임
  폴더(힐러의 스킬·장비 아이콘은 `games/healer/icons.js`), **목록 페이지와 파비콘은
  HTML에 경로를 바로 적는다.** 목록 페이지가 부르는 스크립트는 확대를 막는 공용
  파일 하나뿐이고, 아이콘 때문에 거기에 더 들이지 않는다.
- **HTML에는 이름만 적는다**(`data-icon="pencil"`). 경로를 HTML에도 적으면 같은
  그림이 두 곳에 생겨 한쪽만 고치는 일이 난다.
- **글자는 글자로 둔다.** `←`(목록으로), 문장 속의 `→`, 더블블록의 검은 칸
  표시(`■`)는 아이콘이 아니라 읽는 글자이고, 꼬들의 공유 문구(🟩🟨⬜)는 다른
  앱에 붙여 넣는 텍스트라 그림으로 바꿀 수 없다.
- 오디오 재생은 로드 시 한 번 시도하고(허용된 환경에서는 바로 시작),
  막히면 컨텍스트가 `running`이 되는 순간을 `statechange`로 받아 시작한다.
- **조작을 기다리는 쪽도 공용 뼈대가 맡는다.** `shared/audio.js`가 문서의 캡처
  단계에서 첫 조작(포인터·터치·키·클릭)을 듣고 컨텍스트가 열릴 때까지 계속
  시도한다. **한 번만 듣고 끊으면 안 된다** — 그 한 번의 `resume()`이 통하지
  않는 브라우저가 있어, 힐러에서 실제로 전장을 눌러 스킬을 써야 곡이 시작되는
  일이 있었다. 게임이 따로 `unlock()`을 부를 이유는 없다.
- 오디오 뼈대는 `shared/audio.js` 하나로 모았다. **각 게임의 곡과 효과음은 여기에
  두지 않고 게임 별로 관리한다**
- **도움말은 바텀시트다.** 화면 아래에서 올라오고, 손잡이나 본문을 아래로 밀거나
  가림막을 누르거나 Esc로 닫는다. 껍데기는 `shared/sheet.js`와 `shared/sheet.css`
  하나뿐이고, 게임은 도움말 내용만 들고 있다가 `SharedSheet.bind()`에 넘긴다 —
  껍데기 마크업을 게임마다 두면 한 곳만 고치는 일이 난다. 결과 시트처럼 제자리에
  펼치는 시트는 여기에 해당하지 않는다.
- **브라우저에서 클래식 스크립트는 전역 렉시컬 스코프를 공유한다.** 한 게임이
  파일 여러 개로 나뉘면 각 파일을 IIFE로 감싼다. 감싸지 않으면 파일마다 최상위에
  선언한 같은 이름이 충돌해 페이지 전체가 죽는데, node에서는 모듈 스코프라
  로직 테스트가 전부 통과하고 브라우저에서만 터진다.
- **더블 탭 확대는 모든 페이지에서 막고, 그 규칙은 `shared/base.css`와
  `shared/base.js` 둘이다.** 게임마다 적어 두면 새 게임에서 빠뜨리기 쉬워 뽑아냈다.
  막는 것은 `touch-action: manipulation`이고 — `user-scalable=no`는 iOS 사파리가
  무시한다 — **전체 선택자로 모든 요소에 건다.** html과 body에만 걸었더니 규격과
  달리 실기기에서 그 안의 요소까지 미치지 않았다. 두드린 요소 자신이 값을 들고
  있어야 한다.
- **자바스크립트가 한 겹 더 필요한 이유는 판이다.** 끌어서 조작하는 자리에는
  `touch-action: none`을 걸어야 하는데(`manipulation`은 끌면 페이지가 따라
  스크롤된다), **iOS 사파리는 `none`으로는 더블 탭 확대를 막지 않는다.** 그래서
  화면 대부분은 막히는데 판 안에서만 확대되는 상태가 됐다. `shared/base.js`가 효력이
  `none`인 자리에서 **`touchend`의 기본 동작을 조건 없이** 없앤다. 간격을 재서
  "두 번째 두드림"만 막아 봤지만 사파리가 더블 탭으로 보는 간격이 더 넓어 새 나갔다.
  `touchstart`에서 막는 것도 안 된다 — 2048은 시작과 끝의 좌표 차이로 스와이프를
  읽는데 시작을 취소하면 그 뒤가 오지 않는다.
- **`none`인 자리에는 click으로 도는 조작을 두지 않는다.** 위가 `touchend`를
  취소하므로 그 안에서는 click이 나지 않는다. 포인터·터치 이벤트로 짠다.
- **손가락 밑의 요소를 그 자리에서 갈아 끼우지 않는다.** 눌린 칸의 innerHTML을
  바꾸면 두드림이 시작된 요소가 사라져 `touchend`가 문서까지 올라오지 않는다 —
  위의 처리기가 왕관 놓기에서만 듣지 못한 이유가 이것이었다. 바뀌는 그림은 미리
  넣어 두고 감춘다.
- **손가락을 벌려 키우는 확대는 `shared/base.js`가 막는다.** viewport 메타의
  `user-scalable=no`는 iOS가 무시하고 `manipulation`은 벌리기를 허용하는 값이라,
  웹킷의 gesture 이벤트와 손가락 둘 이상의 `touchmove`를 취소해서 막는다. 사파리
  판에 따라 한쪽이 새는 일이 있어 두 갈래를 다 둔다.
- **목록 페이지도 예외가 아니다.** 한동안 읽는 페이지라고 벌리기를 남겨 뒀지만,
  아이폰에서 확대되는 것이 거슬린다는 이야기를 듣고 똑같이 막기로 했다. 그래서
  목록 페이지도 `shared/base.js`를 부른다.
- 기존 파일을 수정할 때는 그 파일의 관용구와 주석 밀도를 따른다.

## Git 워크플로

기본 브랜치는 `main`.

- **작업은 가급적 새 브랜치를 만들어 진행한다.** `main`에 직접 커밋하지 않는다.
  세션이 작업할 브랜치를 지정받았다면 그 브랜치를 쓴다.
- **하나의 작업이 완료되면 커밋한다.** 커밋 단위는 작업 단위다. 여러 작업을 한
  커밋에 몰지 않고, 완료되지 않은 작업을 커밋하지 않는다.
- **push할지 말지는 커밋이 살아남는지로 갈린다.**
  - 원격(클라우드) 세션에서는 **커밋한 뒤 바로 push하고, 이어서 `main`에 머지까지
    한다.** 컨테이너는 놀려 두면 회수되므로 작업이 브랜치에만 남아 있으면 그
    상태를 사람이 다시 챙겨야 한다. 머지 커밋도 브랜치 커밋과 같은 규칙을 따른다.
  - 로컬 CLI에서는 **커밋까지가 기본이고 push는 요청받을 때만 한다.**
- 브랜치의 PR이 이미 머지됐다면 그 위에 커밋을 쌓지 않는다. 최신 `main`에서 브랜치를
  다시 만들고 새 변경으로 취급한다.

CI가 없으므로 초록색 체크로 정합성을 확인할 수 없다. CI가 생기기 전까지는 로컬에서
가능한 검증을 돌리고, **무엇을 돌렸고 결과가 무엇이었는지** 보고에 그대로 적는다.
작업과 관계없는 다른 게임의 테스트는 실행하지 않는다.
