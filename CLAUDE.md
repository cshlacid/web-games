# CLAUDE.md

이 저장소에서 작업하는 AI 어시스턴트를 위한 지침.

## 언어

- **모든 대화와 코드 주석은 한국어로 작성한다.** 커밋 메시지와 PR 본문도 한국어를
  기본으로 한다.
- 식별자(변수·함수·파일명)는 영어를 유지한다. 언어 규칙은 사람이 읽는 산문에
  적용되며, 코드 자체에는 적용되지 않는다.

## 주석 및 문서 작성 원칙

- **주석은 자명하지 않은 이유(why)만 적는다.** 코드가 무엇을 하는지는 코드가 이미
  말하고 있으므로 다시 적지 않는다. 그 선택을 한 이유, 시도했지만 실패한 대안,
  겉보기에 불필요해 보이는 코드가 필요한 이유가 주석의 대상이다.
- **이 파일도 같은 규칙을 따른다.** 트리를 열어 보면 알 수 있는 것(디렉터리 목록,
  설치된 의존성, 존재하는 파일), 설정 파일에 이미 적혀 있는 것은 여기에 옮겨
  적지 않는다. 이 파일에는 코드를 읽어도 알 수 없는 것만 남긴다 — 결정의 배경,
  아직 정해지지 않은 것, 저장소 밖의 워크플로.
- 규칙을 무효화하는 변경을 할 때는 같은 커밋에서 이 파일을 고친다.

## 프로젝트 성격

**여러 개의 미니 웹게임을 한 저장소에서 관리한다.** 게임 여러 개를 담은 하나의
앱이 아니라, 서로 독립적인 게임들이 한 저장소에 모여 있는 형태다.

- **스택은 게임마다 다를 수 있다.** 저장소 전체에 공통 프레임워크를 강요하지
  않는다. 그 대신 **의존성·빌드 설정은 해당 게임 폴더 안에 갇혀 있어야 한다.**
  한 게임의 선택이 다른 게임의 선택을 제약하는 순간 이 구조의 이점이 사라진다.
- 새 게임의 스택은 그 게임에 맞는 것을 고르되, 정하기 전에 사용자와 합의한다.
- **혼자 가볍게 만들 수 있는 규모의 단일 플레이어 게임**이 대상이다. 서버, 계정,
  실시간 멀티플레이는 범위 밖이다. 정적 파일만으로 배포되는 상태를 유지한다 —
  이 제약이 게임을 계속 추가할 수 있게 하는 조건이다.
- 게임 하나를 추가하는 것이 기본 작업 단위다. 여러 게임을 동시에 건드리는 변경은
  피한다.

게임 하나는 `games/<이름>/` 폴더 하나에 담기고, 루트 `index.html`이 게임 목록
페이지다. **게임을 추가하면 목록 페이지에도 카드를 추가한다** — 링크되지 않은
게임은 사이트에서 도달할 수 없다.

스택은 게임마다 달라도 **헤더 배치는 다섯 게임이 같다**: 왼쪽 위에 `← 목록`(도움말이
있으면 그 옆), 가운데에 게임 이름, 오른쪽에 소리 설정. 나머지는 그 아래 줄에 둔다.
게임을 오가며 놀 때 돌아가는 길을 매번 다시 찾지 않게 하려는 것이므로, 새 게임도
이 배치를 따른다. 지금은 같은 CSS가 게임마다 복사돼 있다 — 공통 CSS로 뽑을지는
아직 정하지 않았다.

`shared/`는 세 번째 게임에서 같은 코드가 세 번 나온 뒤에야 만든 예외다. **여기에는
게임의 내용이 아니라 배관만 둔다.** 무엇을 공유할지 애매하면 넣지 않는다 — 게임이
서로를 제약하기 시작하면 이 저장소 구조의 이점이 사라진다.

## 배포

GitHub Pages가 `main` 브랜치의 루트를 그대로 서빙한다. 여기서 나오는 제약들:

- **`main`에 머지되는 순간 배포된다.** 별도 배포 단계가 없다는 뜻이자, `main`은
  항상 동작하는 상태여야 한다는 뜻이다.
- **경로는 상대경로로 쓴다.** 사이트가 `/web-games/` 하위에 올라가므로 `/style.css`
  같은 절대경로는 로컬에서만 되고 배포된 사이트에서는 깨진다.
- **빌드 산출물이 아니라 저장소 파일이 그대로 서빙된다.** 빌드가 필요한 게임을
  추가하는 순간 이 방식이 깨진다. 그때는 산출물을 커밋하든 Actions 배포로 바꾸든
  결정하고, **이 파일에 먼저 적은 뒤** 옮긴다.

## 명령

**사이트에는 패키지 매니저도 빌드 도구도 없다.** 받아서 열면 그대로 돈다.
예외는 아래 `bake.js` 둘뿐인데, 배포에 끼지 않는 오프라인 도구다.

```bash
python3 -m http.server 8000   # 로컬 확인 (http://localhost:8000)
node games/2048/logic.test.js   # 2048 규칙 테스트
node games/chess-puzzle/logic.test.js # 체스 퍼즐 수순 테스트
node games/chess-puzzle/data.test.js  # 체스 퍼즐 덩이 적재 테스트
node games/kkodle/logic.test.js # 꼬들 자모 처리·판정 테스트
node games/sudoku/logic.test.js # 스도쿠 기법·생성기 테스트
node games/doppelblock/logic.test.js  # 더블블록 규칙·완전 탐색 테스트
node games/doppelblock/solver.test.js # 더블블록 논리 기법·힌트 테스트
node games/doppelblock/generator.test.js # 더블블록 생성기·난이도 테스트

node games/doppelblock/bake.js 8 45 12345 >> /tmp/eight.txt  # 큰 판 미리 굽기
node games/chess-puzzle/bake.js pick                        # 체스 퍼즐 900개 굽기
```

게임별 테스트는 그 게임 폴더 안에 두고, 새로 만들면 여기에 명령을 추가한다.
린트·포매터는 아직 없다.

`bake.js` 둘은 배포에 끼지 않는 오프라인 도구다. 게임 안에서 만들기에는 너무
드물거나 무거운 자료를 미리 만들어 둔다. **하는 일은 서로 다르다.**

더블블록은 판을 **만든다**. 무작위로 뽑아 매겨 보고 버리는데, 8×8은 논리로
풀리는 판이 0.04%뿐이라 한 판에 18초가 든다. 씨앗을 달리해 코어 수만큼 동시에
돌리고 결과를 합치므로 **합칠 때 중복을 다시 걸러낸다** — 프로세스 안에서만
중복을 보기 때문이다.

체스는 판을 **고른다**. Lichess Puzzle Database(CC0)를 훑어 조건에 맞는 것을
추린다. 전에는 여기서도 엔진끼리 두게 하고 실수가 난 자리를 캤는데, 그건
lichess를 받아 올 수 없던 동안의 대체재였다. 자료가 생긴 지금은 쓸 이유가
없어서 지웠고 — 사람이 실제로 둔 판이고, 사람 수천 명이 매긴 난이도(레이팅)와
검증된 수순이 붙어 있다 — **Stockfish 의존도 그와 함께 사라졌다.** 저장소에
`package.json`도 GPL 엔진도 없어야 한다는 제약이 이제 그냥 지켜진다.

고를 때 거는 조건은 셋이다. 품질 지표(찬반·시행 횟수·레이팅 편차)로 한 번
좁히고, 레이팅 구간으로 난이도를 가르고, **대표 주제별로 칸을 나눠 돌아가며
뽑는다.** 마지막 것이 없으면 900개가 메이트와 포크로 채워진다 — 자료에서 그
둘이 압도적으로 많다. 담기 전에 `logic.js`로 직접 풀어 보는 것은 lichess가
검증한 수순이라도 우리 엔진이 못 따라가면 화면에서도 못 풀기 때문이다.

**자료 파일(`lichess_db_puzzle.csv.zst`, 610만 줄 304MB)은 굽는 데만 쓴다.**
<https://database.lichess.org/lichess_db_puzzle.csv.zst> 에서 받아 게임 폴더에
둔다. pzstd로 묶여 있어 압축 프레임 앞마다 건너뛰기 프레임이 끼는데, node의
zstd 스트림은 거기서 멈춘다 — `bake.js`가 프레임을 직접 끊어 푸는 이유다.
**이 파일은 저장소에 두지 않는다** — `.gitignore`가 `*.zst`를 막는다. Pages가
루트를 그대로 서빙하므로, 커밋하면 사이트에 304MB가 그대로 얹힌다. 한 번 커밋된
적이 있었고 히스토리에서도 들어냈다. 그 커밋이 아직 원격에 올라가지 않은
상태였기 때문에 **공유된 SHA는 하나도 건드리지 않고** 정리할 수 있었다 —
`filter-branch`로 전체를 다시 쓰면 `origin/main`과 겹치던 커밋까지 갈리므로,
그 대신 문제의 커밋 위에 쌓인 것만 `rebase --onto`로 옮겼다.

## 규칙

- **게임 로직과 화면을 파일로 분리한다.** 2048이 `logic.js`(순수 함수)와
  `main.js`(DOM·입력)로 나뉜 이유는, 브라우저 없이 `node`로 규칙을 검증하기
  위해서다. 로직이 DOM을 만지는 순간 이 테스트가 불가능해진다.
- 로직 모듈 끝의 `module.exports` / `window.X` 이중 노출은 그 테스트를 위한
  통로다. 빌드 도구 없이 node와 브라우저 양쪽에서 쓰려면 이 방법이 필요하다.
- **체스 퍼즐 자료만 한 번에 다 받지 않는다.** 900개 300KB를 통째로 받으면 첫
  문제 하나를 보려고 899개를 기다리게 되므로, 목차(`puzzles/index.js`)만 미리
  받고 나머지는 그 문제가 나올 때 덩이 단위로 받는다. **`fetch`가 아니라
  `script` 태그를 꽂는다** — `file://`로 열어도 돌아가야 하는데 fetch는 거기서
  CORS에 막힌다. 덩이 파일이 콜백 없이 자기 자신을 전역 표에 등록하는 것도
  같은 이유로 골랐다. node에서 `require` 한 번이면 같은 자료가 모여 테스트가
  브라우저 없이 돈다.
- 그래서 **화면은 전체 문제 목록을 볼 수 없다.** 푼 기록에서 없어진 아이디를
  걸러 내던 것을 목차의 `version` 비교로 바꾼 이유가 이것이다. 전체를 훑어야
  하는 기능을 새로 넣으려면 먼저 이 구조를 다시 봐야 한다.
- **문제 차례를 섞을 때도 덩이 경계를 지킨다.** 덩이 차례를 섞고 덩이 안을
  따로 섞는다 — 900개를 한꺼번에 섞으면 다음 문제가 거의 항상 다른 덩이에
  있어서 문제마다 파일을 하나씩 받게 되고, 위 구조가 그대로 무너진다. 덩이는
  주제를 돌아가며 담은 것이라 한 덩이 안에 눈에 띄는 공통점이 없다.
- 섞은 차례는 **씨앗만 저장하고 다시 만든다.** 매번 새로 섞으면 저장해 둔
  번째가 다른 문제를 가리켜 "이어서 풀기"가 성립하지 않는다.
- **"이어지는 수순 보기"는 지금 아무 문제에도 안 뜬다.** 그 기능이 쓰는 `line`은
  엔진 PV라서 Stockfish 없이는 만들 수 없다. 화면 코드는 남겨 뒀다 — `line`이
  다시 생기면 그대로 살아난다. 같이 넣었던 `why`는 엔진 없이 만들 수 있어서
  살아 있다(수순을 끝까지 둔 자리의 물질 차이 + lichess의 판정).
- **소리는 음원 파일이 아니라 Web Audio로 합성하고, 그림은 인라인 SVG로 직접
  그린다.** 저장소에 바이너리 애셋을 들이지 않고, 빌드 없이 정적 파일만 배포하는
  구조를 유지하기 위해서다. 라이선스를 따질 일이 없어지는 것도 이 선택의 이유다.
  체스 말은 이 규칙을 따른 첫 그림이다 — 유니코드 기호로 그렸더니 글꼴에 따라
  모양이 달라지고 심볼 글꼴이 없는 기기에서는 네모로 나왔다.
- **브라우저는 사용자 조작 전 오디오 재생을 막는다.** 우회할 수 없으므로 소리를
  넣는 게임은 두 경로를 모두 둔다: 로드 시 한 번 시도하고(허용된 환경에서는 바로
  시작), 막히면 컨텍스트가 `running`이 되는 순간을 `statechange`로 받아 시작한다.
  `resume()`의 반환값에 기대면 안 된다 — 프라미스를 돌려주지 않는 브라우저가 있다.
- 오디오 뼈대는 `shared/audio.js` 하나로 모았다. 게임 셋에 같은 코드가 세 번
  생긴 뒤에 뽑은 것이고, 그 전까지는 일부러 복사해 뒀다. **곡과 효과음은 여기에
  두지 않는다** — 게임마다 완전히 다르고, 여기 섞이는 순간 한 게임의 소리가
  다른 게임을 제약한다. 각 게임의 `audio.js`는 곡·효과음 정의만 담는다.
- **브라우저에서 클래식 스크립트는 전역 렉시컬 스코프를 공유한다.** 한 게임이
  파일 여러 개로 나뉘면 각 파일을 IIFE로 감싼다. 감싸지 않으면 파일마다 최상위에
  선언한 같은 이름이 충돌해 페이지 전체가 죽는데, node에서는 모듈 스코프라
  로직 테스트가 전부 통과하고 브라우저에서만 터진다.
- 기존 파일을 수정할 때는 그 파일의 관용구와 주석 밀도를 따른다.
- 게임이 몇 개 더 쌓여 공통 스타일이 실제로 반복되면 그때 공유를 논의한다.
  미리 공통 CSS를 만들지 않는다 — 게임별 스택 자유와 충돌한다.

## Git 워크플로

기본 브랜치는 `main`.

- **작업은 가급적 새 브랜치를 만들어 진행한다.** `main`에 직접 커밋하지 않는다.
  세션이 작업할 브랜치를 지정받았다면 그 브랜치를 쓴다.
- **하나의 작업이 완료되면 커밋한다.** 커밋 단위는 작업 단위다. 여러 작업을 한
  커밋에 몰지 않고, 완료되지 않은 작업을 커밋하지 않는다.
- **push할지 말지는 커밋이 살아남는지로 갈린다.**
  - 원격(클라우드) 세션에서는 **커밋한 뒤 바로 push한다.** 컨테이너가 회수되면
    push하지 않은 커밋은 함께 사라진다 — 여기서 물어보고 기다리는 것은 작업을
    잃을 위험을 사용자에게 떠넘기는 것이다.
  - 로컬 CLI에서는 **커밋까지가 기본이고 push는 요청받을 때만 한다.** 커밋이
    디스크에 남으므로 서두를 이유가 없고, 무엇을 원격에 올릴지는 사용자가
    고르게 둔다.
  - 어느 쪽이든 `git push -u origin <branch-name>` 형태로 하고, 네트워크
    오류일 때만 지수 백오프(2s, 4s, 8s, 16s)로 재시도한다.
- **push는 자동이어도 머지와 PR은 아니다.** `main` 머지도, PR 열기도 명시적으로
  요청받았을 때만 한다.
- 브랜치의 PR이 이미 머지됐다면 그 위에 커밋을 쌓지 않는다. 최신 `main`에서 브랜치를
  다시 만들고(`git fetch origin main && git checkout -B <branch> origin/main`) 새
  변경으로 취급한다.

CI가 없으므로 초록색 체크로 정합성을 확인할 수 없다. CI가 생기기 전까지는 로컬에서
가능한 검증을 돌리고, **무엇을 돌렸고 결과가 무엇이었는지** 보고에 그대로 적는다.
테스트가 실패하면 출력을 함께 싣고, 건너뛴 단계가 있으면 어느 단계인지 밝힌다.
