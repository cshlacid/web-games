'use strict';

// 캐릭터와 적의 자료. 규칙은 이 표만 보고 돌아간다 — 새 캐릭터를 들일 때
// `rules.js`를 건드리지 않기 위해서다.
//
// **판 안의 경제는 스테이지가 올라도 그대로다.** 고용비도 잡았을 때 받는 골드도
// 고정이고, 스테이지마다 오르는 것은 적 체력뿐이다. 둘 다 올리면 숫자만 커지고
// 판단은 똑같아진다.
(function () {

// 사거리는 칸, 발사 속도는 초당 횟수, 체력은 적이 부술 때 깎는 값이다.
//
// 값에 이론은 없다. 웨이브 열 개를 3~4분에 끝내는 것을 목표로 잡은 출발점이고,
// 실제 값은 `balance.test.js`의 자동 플레이로 맞춘다.
// **사거리는 안쪽과 바깥쪽 둘이다.** 포수는 2~5라 코앞의 적을 못 친다 — 바깥에서
// 때리는 자리와 앞을 막는 자리가 갈리는 것이 이 게임의 배치다. `min`은 사람이 읽는
// 값이고 판정에서 쓰는 안쪽 한계는 `min - 0.5`다.
//
// **모두 기본 공격과 스킬 둘을 가진다.** 기본 공격은 한 놈을 때리는 것이고(`basic`을
// 적으면 범위로도 된다), 스킬은 제 쿨타임이 돌아올 때 그 자리를 대신한다. **쿨타임 중에는 가만히 있지 않고 기본
// 공격을 한다** — 기다리는 동안 아무것도 못 하면 쿨타임이 긴 캐릭터는 세울 이유가
// 없어진다.
//
// `skill.mul`은 기본 공격 대비 배수다. 값에 이론은 없고 `balance.test.js`의 자동
// 플레이로 맞춘다.
// 이름과 설명은 여기 두지 않는다 — 여섯 언어로 갈리므로 id를 사전 열쇠로 쓴다.
const UNITS = {
  archer: {
    cost: 60, damage: 9, rate: 1.1, hp: 60,
    range: { min: 1, max: 4 },
    skill: { cd: 5, shape: 'single', mul: 2.4 },
  },
  shield: {
    front: true, cost: 70, damage: 5, rate: 1.0, hp: 320,
    range: { min: 1, max: 1 },
    skill: { cd: 3.5, shape: 'single', mul: 1.2, stunFor: 1.2 },
  },
  cannon: {
    cost: 110, damage: 10, rate: 0.8, hp: 70,
    range: { min: 2, max: 5 },
    // **기본 공격도 작은 범위다.** 쿨타임 6초짜리라 대부분의 시간을 한 놈씩 때리고
    // 있었고, 그러면 이름만 포수다. 반지름 1이라 맞닿은 칸까지만 번져 운석(2)과
    // 겹치지 않는다. 무리 판에서 포수가 답이 되는 자리가 여기서 나온다.
    basic: { shape: 'splash', splash: 1 },
    skill: { cd: 6, shape: 'splash', mul: 2.6, splash: 1.2 },
  },
  frost: {
    cost: 90, damage: 6, rate: 0.9, hp: 55,
    range: { min: 1, max: 3 },
    skill: {
      cd: 9, shape: 'field', mul: 0.5,
      fieldR: 1.3, fieldDps: 0.8, fieldFor: 4, slow: 0.45,
    },
  },
  spear: {
    cost: 90, damage: 10, rate: 0.9, hp: 110,
    range: { min: 1, max: 2 },
    skill: {
      cd: 4, shape: 'line', mul: 1.4, bleedDps: 0.65, bleedFor: 3,
    },
  },
  healer: {
    cost: 100, damage: 5, rate: 1.0, hp: 70,
    range: { min: 1, max: 3 },
    // 때리는 것은 신성이다. 산 것에게는 5짜리 잡타지만 **언데드에게는 두 배**로
    // 들어가고 언데드의 두꺼운 저항도 타지 않는다 — 일반 여섯 중 언데드를 맡는
    // 자리가 여기다.
    basic: { shape: 'single', kind: 'holy' },
    skill: {
      cd: 3.5, shape: 'heal', heal: 30, smite: 1.6,
    },
  },
};

// **영웅끼리는 공명한다.** 하나가 스킬을 쓰면 둘레 `r`칸 안의 **다른 영웅**의
// 쿨타임이 `cut`초 줄어든다. 영웅 칸을 여럿으로 늘리면서 넣은 규칙이고, 없으면
// 둘째·셋째는 "조합"이 아니라 그냥 "여분"이다 — 실제로 검성+대마법사의 결과가
// 대마법사 혼자와 똑같았다.
//
// **붙여 세워야 걸리는 것이 핵심이다.** 대마법사는 코앞을 못 치고 체력이 종잇장이라
// 뒤에 서고 싶은데, 공명을 받으려면 검성 곁으로 와야 한다 — 그 긴장이 배치를
// 판단으로 만든다.
// **영웅은 다른 영웅 곁에 서면 세진다.** 한 명당 피해와 쿨타임 속도가 +50%이고
// 사령관은 두 명분으로 친다 — 둘이면 ×1.5, 셋이면 ×2, 사령관을 끼면 ×2.5다.
//
// 여기까지 오는 데 두 번 고쳤다. ①처음에는 스킬을 쓸 때마다 둘레 영웅의 쿨타임을
// 몇 초씩 깎았는데, 남은 쿨타임보다 깎는 값이 크면 그 몫이 버려져 둘을 붙여 세워도
// 스무 초에 스킬이 한 번 더 나가는 데 그쳤다(5회 → 6회). ②비율로 바꾸니 스킬은
// 두 배로 나갔지만(5회 → 11회) 판의 결과는 1%만 움직였다 — **영웅이 하는 일의
// 대부분은 스킬이 아니라 기본 공격**이고 거기에는 안 걸렸기 때문이다. 그래서
// 쿨타임과 피해 **둘 다**에 건다.
const BOND = { r: 2.4, gain: 0.5 };

// **영웅은 골드로 사지 않는다.** 골드로 사는 동안에는 둘째 영웅이 영영 값을 못 했다 —
// 값을 절반으로 내려도, 자리를 좁히고 성능을 여섯 배로 올려도, 그 골드로 못 세운
// 일반 서넛과 맞바꾸는 것이라 총량이 그대로였다(영웅 칸 1→2에서 필요 레벨 합
// 504 → 499). 그래서 **축을 아예 갈랐다** — 영웅은 공짜로 부르되 **웨이브마다
// 하나씩만** 부를 수 있다. 일반 편성은 골드 싸움이고 영웅은 시간 싸움이다.
//
// 그러면 "몇 명을 데려가느냐"가 보석(영웅 칸)의 문제가 되고, "어디에 붙여 세우느냐"가
// 공명의 문제가 된다. 골드는 둘 어디에도 끼지 않는다.
const HERO_CALL = { from: 2, gap: 1 };

// 영웅. **영웅마다 한 판에 한 번 부를 수 있다**(`hero`). 값이 비싸 첫 웨이브부터 세울 수
// 없고, 어느 웨이브에 꺼내느냐가 그 판의 판단이 된다. 편성에서 일반 다섯 칸과는
// 따로 한 칸을 차지한다.
// **한 명뿐이므로 일반과 같은 값당 효율이면 데려갈 이유가 없다.** 실제로 그랬다 —
// 값당 초당 피해가 대마법사 24.5, 성기사 5.6으로 궁수(22.1)·포수(25.5)와 같거나
// 아래였고, 스테이지 40까지 누적 판 수가 영웅 없이 166, 검성 174(손해),
// 대마법사 145, 성기사는 스테이지 1에서 막혔다. 그래서 **셋 다 제 자리에서는
// 일반의 두세 배**가 되게 올리고, 대신 그 자리를 좁게 못 박았다.
const HEROES = {
  blade: {
    front: true, cost: 0, damage: 46, rate: 1.3, hp: 620, hero: true,
    range: { min: 1, max: 1 },
    // **가까운 소수를 처리하는 자리다.** 기본도 스킬도 자기를 가운데로 삼아 둘레를
    // 베고, 반지름이 좁아 맞닿은 것만 맞는다(1.1은 대각선 1.41을 뺀다). 넓히지 않는
    // 것이 성격이다 — 멀리 있는 다수는 대마법사의 자리다.
    basic: { shape: 'ring', ring: 1.1 },
    skill: { cd: 4.5, shape: 'ring', mul: 2.2, ring: 1.6 },
  },
  arch: {
    cost: 0, damage: 40, rate: 0.6, hp: 90, hero: true,
    range: { min: 2, max: 6 },
    // **멀리 있는 다수를 처리하는 자리다.** 기본부터 범위이고 반지름이 넓다. 대신
    // 코앞(두 칸 안)은 못 치고 체력이 종잇장이라 앞에 세울 수 없다.
    basic: { shape: 'splash', splash: 1.8 },
    skill: {
      cd: 9, shape: 'splash', mul: 4, splash: 2.6, bleedDps: 0.5, bleedFor: 3,
    },
  },
  // **혼자서는 영웅 중 제일 약하고, 곁에 누가 있을수록 세진다.** 영웅 칸을 여럿으로
  // 늘리면서 그 구조의 축으로 들인 자다 — 호령이 주는 것은 피해가 아니라 시간이라,
  // 받을 사람이 없으면 값이 0이다. 값이 제일 싼 이유도 그것이다.
  warden: {
    lead: true, cost: 0, damage: 14, rate: 1, hp: 280, hero: true,
    range: { min: 1, max: 5 },
    skill: {
      cd: 5, shape: 'rally', mul: 1.2, cut: 2.2, heroCut: 4.5,
    },
  },
  saint: {
    front: true, cost: 0, damage: 26, rate: 1, hp: 300, hero: true,
    range: { min: 1, max: 4 },
    // **언데드를 처리하는 자리다.** 때리는 것이 신성이라 언데드에게 두 배로
    // 들어가고 그들의 두꺼운 저항을 타지 않는다. 산 것에게는 평범한 값이고,
    // 언데드 판에서만 압도적인 것이 이 영웅의 성격이다.
    basic: { shape: 'smite', heal: 0.9, kind: 'holy' },
    skill: {
      cd: 6, shape: 'heal', heal: 140, all: true, smite: 3.2,
    },
  },
};

Object.assign(UNITS, HEROES);
const HERO_KEYS = Object.keys(HEROES);
// 영웅 카드는 이 스테이지부터 나온다. 처음 몇 판은 일반 캐릭터를 하나씩 배우는
// 구간이라 그 앞에 섞으면 배울 것이 두 겹이 된다.
const HERO_FROM = 4;

// 처음 열려 있는 것은 궁수 하나뿐이다. 나머지는 목표를 달성해야 들어온다 —
// 초반 대여섯 스테이지가 하나씩 배우는 구간이 된다.
const STARTER = 'archer';
const LOCKED = ['shield', 'cannon', 'frost', 'spear', 'healer'];

// 판 안에서 올리는 단계. 판이 끝나면 사라진다.
// 판 안에서 올리는 단계. **세기만 오르는 것이 아니라 리듬도 바뀐다.**
//
// 피해만 올렸더니 빙결술사와 대마법사는 올려도 체감이 없었다 — 이들의 값은 한 방의
// 크기가 아니라 바닥을 얼려 두는 시간에 있는데, 쿨타임 9초에 유지 4초는 단계를
// 올려도 그대로였다. **쿨타임은 줄고 지속은 늘어난다**(3단계에서 쿨타임 0.72배,
// 지속 1.56배). 쿨타임이 긴 쪽이 가장 크게 득을 보는 규칙이라, 손볼 자리가 하나로
// 끝난다.
const UP = {
  max: 3,
  cost: [1.0, 1.6],   // 2단계, 3단계로 올리는 값 (고용비 배수)
  damage: 1.6,
  hp: 1.5,
  range: 0.4,         // 바깥쪽만 늘어난다. 안쪽 한계는 그대로 둔다
  cd: 0.85,           // 단계마다 스킬 쿨타임에 곱한다
  hold: 1.25,         // 단계마다 지속 시간(얼음·출혈·기절)에 곱한다
};

// 공격이 닿는 종류. `resist`는 `single`에만 걸린다 — 범위·관통·지속은 제값이
// 들어간다. **지속 피해에는 최소 1이 붙지 않는다**(직접 때리는 값에만 붙는 바닥이라,
// 그대로 두면 틱마다 1이 들어가 초당 서른이 된다).
// **신성은 언데드를 태우는 네 번째 종류다.** 산 것에게는 다른 종류와 다를 것이
// 없고(어느 적도 저항을 적어 두지 않았다), 언데드에게만 두 배로 들어간다.
// 힐러와 성기사가 이 종류로 때린다 — 되살리는 쪽이 언데드를 맡는 자리다.
const HIT = { single: 'single', area: 'area', dot: 'dot', holy: 'holy' };

// 적. hp는 그 스테이지 기준 체력에 곱하는 배수다.
//
// **`resist`는 공격의 종류마다 다른 감소율이다**(`single`·`area`·`dot`). 수치로 된
// 방어 하나는 소용이 없었다 — 보석을 한 캐릭터에 몰면 레벨 60을 넘겨 어떤 방어값도
// 뚫는다. 그래서 막는 것을 값이 아니라 **공격의 종류**로 바꿨다. 적어 두지 않은
// 종류는 0이다.
//
// **표로 넓힌 것은 답을 적마다 다르게 두기 위해서다.** 숫자 하나일 때는 갈리는
// 적이 중장병 하나뿐이라, 궁수 여섯이면 나머지 다섯 종류가 전부 풀렸다.
//
// siege(초당 피해)와 bias(부수는 시간에 곱하는 성향)가 길찾기의 성격을 만든다.
// 공성병은 둘 다 극단이라 조금만 돌아가야 해도 뚫고 들어온다.
const FOES = {
  grunt:   { speed: 1.1, hp: 1, resist: {}, siege: 12, bias: 1, bounty: 8, cost: 1 },
  // **한 놈씩 쏘면 반이 낭비된다.** 체력이 3분의 1이라 한 발이 거의 다 넘치는데,
  // 그 낭비가 마릿수만큼 쌓인다 — 범위로 쓸어야 하는 자리다. 금방 죽어 지속 피해도
  // 다 들어가기 전에 끝난다.
  swarm:   { speed: 1.35, hp: 0.32, resist: { single: 0.5, dot: 0.3 }, siege: 6, bias: 1, bounty: 4, cost: 0.5 },
  // 흩어져 달려 범위에 여럿이 안 걸린다. 그리고 **빠르다** — 사거리 넉 칸을
  // 1.4초에 지나가 느리게 하지 않으면 한두 발밖에 못 넣는다. 답이 둘인 유일한
  // 자리다: 한 놈씩 세게 때리는 자와 발을 묶는 자를 같이 세워야 한다.
  swift:   { speed: 2.8, hp: 0.55, resist: { area: 0.35 }, siege: 8, bias: 1.2, bounty: 7, cost: 0.9 },
  // 한 놈씩 때리는 공격은 10%만 들어간다. 궁수를 아무리 키워도 이 벽은 안 넘는다.
  // **범위도 반만 들어간다** — 범위 하나로 무리와 중장병이 같이 풀리면 포수만 여섯
  // 세우는 것이 새 정답이 된다(실제로 포수만으로 서른 스테이지를 갔다). 여기 남는
  // 답은 방어를 통째로 무시하는 지속 피해다 — 창병의 출혈과 얼음 지대.
  armored: { speed: 0.8, hp: 1.5, resist: { single: 0.9, area: 0.85 }, chill: 0.6, siege: 14, bias: 1, bounty: 22, cost: 1.8 },
  // **여기서만 궁수가 답이다.** 두꺼운 가죽이 범위와 지속을 다 먹지만 한 놈씩
  // 꽂는 것에는 거의 그대로 뚫린다. 초당 60으로 사람을 부수며 들어오므로 시간을
  // 벌 방패병이 같이 필요하다 — 판이 편성을 요구하는 자리가 여기다.
  breaker: { speed: 0.85, hp: 1.4, resist: { single: 0.1, area: 0.5, dot: 0.9 }, chill: 0.25, siege: 60, bias: 0.4, bounty: 16, cost: 2 },
  // 초당 최대 체력의 6%를 되살린다. 조금씩 깎아서는 따라잡지 못하고, 어느 종류로
  // 때려도 얼마쯤은 깎인다 — **한 번에 크게 넣는 스킬**이 답인 자리다.
  mender:  { speed: 1, hp: 1, resist: { single: 0.35, area: 0.35, dot: 0.5 }, siege: 10, bias: 1, bounty: 15, heal: 0.06, healRange: 1.8, cost: 1.9 },
  // **언데드 둘.** 산 것을 잡는 수단이 전부 어정쩡하게 통하는 대신 신성에는
  // 두 배로 탄다(`resist.holy`가 음수다 — 음수는 저항이 아니라 약점이다).
  // 되살리는 쪽(힐러·성기사)이 여기서 딜러가 되는 것이 이 계열의 뜻이다.
  //
  // 저항을 고르게 준 것은 일부러다. 한 종류만 크게 막으면 나머지로 밀어 버리면
  // 되는데, 고르게 막으면 **신성을 데려왔는가**가 그 판의 답이 된다.
  bone:    { undead: true, speed: 1.0, hp: 1.3, resist: { single: 0.55, area: 0.55, dot: 0.7, holy: -1 }, chill: 0.5, siege: 14, bias: 1, bounty: 20, cost: 1.6 },
  // 형체가 없어 때리는 것이 거의 지나간다. 빠르고 사람을 부수러 돌아온다.
  wraith:  { undead: true, speed: 1.9, hp: 0.8, resist: { single: 0.75, area: 0.75, dot: 0.75, holy: -1.2 }, chill: 0.3, siege: 18, bias: 1.3, bounty: 24, cost: 1.9 },
  boss:    { speed: 0.7, hp: 12, resist: { single: 0.55, area: 0.2 }, chill: 0.2, siege: 70, bias: 0.7, bounty: 90, cost: 0 },
};

// 종류가 처음 나오는 스테이지. 숫자만 커지면 웨이브 10과 웨이브 80에서 하는 일이
// 같아져 무한이 지루함이 된다 — 대응이 바뀌는 자리를 여기서 만든다.
const FOE_FROM = { grunt: 1, swarm: 2, swift: 3, armored: 5, breaker: 7, bone: 8, mender: 10, wraith: 13 };

// **같은 종류는 판에 여섯까지만 세운다.** 값으로 막으려 해 봤지만(겹칠수록 비싸게,
// 방어를 세게, 적을 세게) 전부 칼날 위였다 — 한 종류만 키우면 레벨이 두 배로
// 올라가 어떤 수치든 결국 넘긴다. 마릿수를 못 박는 것이 유일하게 확실하다.
// 여섯이면 한 종류로는 스물두 마리 무리도 중장병 일곱도 감당이 안 되고, 다섯
// 종류를 섞으면 서른 명이라 넉넉하다.
const MOST_OF_KIND = 4;

// 그 안에서도 겹쳐 세울수록 조금씩 비싸진다. 한도에 닿기 전에도 섞는 쪽이 싸다.
const RAISE = 1.18;

// 판 하나의 뼈대.
const RUN = {
  waves: 10,
  lives: 5,
  gold: 160,
  ready: 12,      // 첫 웨이브까지
  gap: 8,         // 웨이브 사이
  refund: 0.6,    // 해고했을 때 돌려받는 비율
};

const Data = { UNITS, HEROES, HERO_KEYS, BOND, HERO_CALL, HERO_FROM, STARTER, LOCKED, UP, HIT, RAISE, MOST_OF_KIND, FOES, FOE_FROM, RUN };

if (typeof module !== 'undefined' && module.exports) module.exports = Data;
if (typeof window !== 'undefined') window.DefenseData = Data;

})();
